# Optional: add your Groq API key for AI-powered natural language search
# Get a free key at https://console.groq.com/
# Without this key, the app uses built-in keyword rules for NL search (all features still work)
GROQ_API_KEY=gsk_your_key_here

# Port for local server (default: 5000)
PORT=5000
